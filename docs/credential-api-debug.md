# Credential API connectivity patch

This placeholder file proves that `fix-credential-connection` now differs from `main` so that a Pull Request can be opened.

The actual code changes are already staged in this branch and described in the PR body.